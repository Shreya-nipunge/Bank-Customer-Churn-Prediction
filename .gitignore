
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
history.db
# *.pkl is removed to allow the model file to be pushed for deployment
!.gitignore
!.gitkeep
# Exclude model if it's too large, but for Streamlit Cloud we might need it.
# churn_model.pkl is ~6MB, so it's fine for GitHub.
